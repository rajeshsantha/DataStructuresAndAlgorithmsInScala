package com.concept.scala.hackerrank.functionalProgramming

/** *
  *
  * @see https://www.hackerrank.com/challenges/lists-and-gcd/problem
  *
  */
object ListsandGCD {
  def main(args: Array[String]) {
    /*   var hashmap = HashMap[Int, Int]()

       for (_ <- 1 to readLine.toInt) {

         val t = readLine.split(" ").map(_.toInt).grouped(2).map(x => (x(0), x(1))).toMap
         if (hashmap.isEmpty)
           hashmap ++= t
         else {

           (hashmap.keySet -- t.keySet).foreach(k => hashmap -= k)
           (hashmap.keySet & t.keySet).foreach(k => hashmap(k) = hashmap(k) min t(k))
         }
       }
       println(hashmap.toList.sorted.map(x => s"${x._1} ${x._2}").mkString(" "))
   */
  }

}
